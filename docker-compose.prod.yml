# EC2 production API — image is set at deploy time (ECR URI + tag).
# Expects host env file at /etc/tracker.env (DATABASE_URL, email, LLM keys, etc.).
services:
  api:
    image: "${TRACKER_IMAGE}"
    ports:
      - "8000:8000"
    env_file:
      - /etc/tracker.env
    restart: unless-stopped
